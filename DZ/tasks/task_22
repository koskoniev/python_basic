ДЗ 22. Уникальное число

Вам необходимо написать функцию find_unique_value , принимающую список из чисел, находит среди них уникальное число и возвращать его. Уникальное число – это число, которое встречается в списке один раз. Если в одном списке будет несколько уникальных чисел, проверять не нужно.

Пример:

Copy code
def find_unique_value(some_list):
    pass

assert find_unique_value([1, 2, 1, 1]) == 2, 'Test1'
assert find_unique_value([2, 3, 3, 3, 5, 5]) == 2, 'Test2'
assert find_unique_value([5, 5, 5, 2, 2, 0.5]) == 0.5, 'Test3'
print("ОК")
