ДЗ 10. Имя переменной

Пользователь вводит строку. Ваша задача – проверить, может ли эта строка быть именем переменной.

Переменная не может:

    начинаться с цифры,
    состоять только из цифр,
    содержать заглавные буквы, пропуск и знаки пунктуации (взять можно здесь string.punctuation), кроме нижнего подчеркивания "_".
    быть ни одним из зарегистрированных слов.
При этом полное имя переменной может состоять только из одного нижнего подчеркивания "_". Подразумевается, что ПОЛНОЕ имя переменной может быть только с одного символа "_" Более одного нельзя. То есть если пользователь ввел __ , это уже ошибка

Если этот символ используется для соединения слов в названии переменной – то таких символов может быть множество

Список зарегистрированных слов можно взять из keyword.kwlist.

В результате проверки на печать выводится либо True, если такое имя переменной допустимо, либо False – если нет.

True или False не нужно переводить из булевого типа данных в строку.

То есть должно быть

print( False ) или print( True ) в коде.

print( "False" ) или print( "True" ) - это неверно!

Примеры имен переменных и результат проверки (=> на печать выводить не нужно :))

Copy code
_ => True
x => True
get_value => True
get value => False
get!value => False
some_super_puper_value => True
Get_value => False
get_Value => False
getValue => False
3m => False
m3 => True
assert => False
assert_exception => True
